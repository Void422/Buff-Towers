# Optional for local development. On Render, add these in Environment.
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Optional: change the Redis key used for tower state.
TOWER_STATE_REDIS_KEY="buff-towers:snapshot"
